You are working in a Node.js monorepo (TypeScript, npm workspaces).

1. Run from the repository root: `npm run ci` (this runs build, unit tests, HTTP regression, and Playwright E2E).
2. If everything passes, reply with a one-line summary "CI OK" and stop.
3. If anything fails: read the error output, make the smallest correct code or config changes, then run `npm run ci` again. Repeat only as needed.
4. Constraints: do not add secrets or real API keys; do not weaken tests to make them pass without a real fix; do not change unrelated packages unless required for the failure.
5. When done fixing, summarize what you changed in bullet points.

Repository root is the current working directory.
