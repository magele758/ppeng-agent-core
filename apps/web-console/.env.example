# Next Agent Lab → daemon 反向代理目标（必填；与根目录一键 dev 可省略，脚本会写入）
# DAEMON_PROXY_TARGET=http://127.0.0.1:7070
#
# 若 daemon 启用了 RAW_AGENT_AUTH_TOKEN，填相同值以便 middleware 在服务端代为附加
# Authorization: Bearer（令牌不会打进浏览器 bundle）。仅用 `npm run dev:web-console` 时常需在此填写；
# `npm run dev`（根目录 dev-lab）会从仓库根 .env 继承 process.env。
# RAW_AGENT_AUTH_TOKEN=
